import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mytestAppPhilBoisvert.App',
  appName: 'capacitor',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
